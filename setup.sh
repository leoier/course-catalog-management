#!/bin/bash

# setting up environment variables for local run and testing

export DATABASE_URL='{Your database url here}'

export TEST_DATABASE_URL='{Your test database url here}'

export DIRECTOR_TOKEN='{Generate a token as director}'

export INSTRUCTOR_TOKEN='{Generate a token as instructor}'
